Bootstrap: docker
From: conda/miniconda3

%help

%labels
  AUTHOR_NAME Alex Harral
  AUTHOR_EMAIL aharral@wistar.org
  VERSION 1.0

%environment
  # set system locale
  export LC_ALL='C'

%post -c /bin/bash
  apt-get -y update && apt-get -y upgrade
	apt-get -y install neofetch wget

  

%runscript
	cat /etc/os-release
