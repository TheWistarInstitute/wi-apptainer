Bootstrap: docker
From: r-base:3.6.3

%help

%labels
  AUTHOR_NAME Alex Harral
  AUTHOR_EMAIL aharral@wistar.org
  VERSION 1.0

%post -c /bin/bash
  # install R packages
  R -e 'install.packages("BiocManager", repos="https://cran.rstudio.com")'
  R -e 'install.packages(c("dplyr", "tidyverse"))'
  R -e 'BiocManager::install("DiffBind", version = "3.10")'

%runscript

%test
