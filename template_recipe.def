Bootstrap: docker
From: ubuntu:latest

%help
  This container is a template to start building. To run the script:

    $ apptainer run ubuntu_latest.sif  # or ./ubuntu_latest.sif

%labels
  AUTHOR_NAME Your name
  AUTHOR_EMAIL email@wistar.org
  VERSION 1.0

%environment
  # set environment variables
  export LC_ALL='C'

%post -c /bin/bash
  apt -y update && apt -y upgrade
	apt -y install fastfetch

%runscript
	fastfetch
